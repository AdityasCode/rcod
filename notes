changed the default arg for model to python main.py to be IF not none
diffdist and faiss gpu and pytorch cuda gpu necessary in req.yml
fixed req.yml to install pkgs and gpu versions
created a working job sub script
installed checkpts
fixed technical errors
cleaned up logs

11/2
given that rcod is post hoc, it should already be model agnostic given the input directly
landseer now takes output, which becomes save_path, which is converted to id_file, used to save each result
